apiVersion: batch/v1
kind: Job

metadata:
  name: platform-maintenance
  namespace: python-web-app

spec:
  template:
    spec:
      containers:
        - name: maintenance
          image: busybox
          command:
            - sh
            - -c
            - echo "Starting platform maintenance"; sleep 5; echo "Maintenance completed"

      restartPolicy: Never
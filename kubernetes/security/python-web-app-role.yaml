apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: python-web-app-pod-reader
  namespace: python-web-app

rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - get
      - list
      - watch